# Berichte der Aufräumskripte. Sie enthalten Verkaufschancen-Namen und damit
# Kundenbezug - das Repo ist öffentlich. Die Datei gehört zur Durchsicht auf
# den Rechner, nicht hierher.
aufraeumen-b1-bericht.csv
*-bericht.csv

# Abhängigkeit des Cron (SheetJS). Wird in GitHub Actions bei jedem Lauf
# frisch installiert - im Repo hat sie nichts zu suchen.
cron/node_modules/
